# 王者本地开发

## 另一台电脑首次启动

完整前置依赖为：Bash、`make`、`mkfifo`、Go `>=1.25`（默认 toolchain 自动下载由 `go.mod` 固定的 `go1.26.7`）、本机原生 PostgreSQL 服务及 `psql`、`createdb`、`pg_isready` 客户端、`curl`、`jq`、`lsof`，以及仓库 `.nvmrc` 指定的 Node.js `24.20.0`（支持范围 `>=24.20.0 <25`）和 npm `>=11.19.0 <12`。macOS 可先执行 `xcode-select --install` 安装 `make`，Bash 和 `mkfifo` 由系统提供；再用 Homebrew 安装 Go、PostgreSQL、jq，系统自带 curl/lsof。Node 建议安装 nvm 后在仓库根目录执行 `nvm install && nvm use`，再用 `npm install -g npm@11.19.0` 对齐 npm。Linux 请用发行版包管理器安装 Bash、make、coreutils（提供 `mkfifo`）、PostgreSQL 服务/客户端、curl、jq、lsof，并从 Go 官方发行版或受信软件源安装符合版本的 Go。启动 PostgreSQL 后执行：

```bash
make dev-init
make dev
```

> **安全警告：** 用户端和后台按需求监听 `0.0.0.0`，而四个体验账号使用固定密码。只能在受防火墙保护的可信本机/局域网运行，绝不能把 5173、5174、8080 或 PostgreSQL 端口暴露到公网。

也可以一次执行 `make dev-first-run`；初始化成功后它会继续启动三个服务并保持在前台。默认连接为 `localhost:5432/wangzhe`、数据库用户 `postgres`、本地密码 `123456`、`debug` 模式；另一台电脑的 PostgreSQL 密码不同时须通过环境变量或环境文件覆盖。该 PostgreSQL 用户必须能连接维护库 `postgres`；当应用数据库尚不存在时还须有 `CREATEDB` 权限。脚本不会创建或修改 PostgreSQL 角色及其密码。

`make dev-init` 是非破坏性的首次安装流程：按锁文件执行两套前端的 `npm ci`、下载 Go 模块、仅在 `wangzhe` 不存在时用干净的 `template0` 创建它，然后执行 SQL 迁移和显式的本地验收初始化。它不会删除、清空、重命名或覆盖已有数据库。脚本先按当前 PostgreSQL 集群内的数据库名取得会话级 advisory lock，并在锁内重新检查数据库、收据和数据库注释；并发的第二次初始化会停止，不能覆盖已有有效收据。该锁由一个仅连接本机维护库的持久 psql 会话持有，脚本正常退出、SIGKILL、连接断开或电脑断电时 PostgreSQL 自动释放，不会留下永久阻塞的陈旧锁。

新库只在 `createdb` 明确成功后才生成恢复收据，收据同时绑定 PostgreSQL 集群标识、数据库名和实际数据库 OID，随后立即写入“初始化中”数据库注释。如果 `createdb` 失败或结果不确定，脚本不留可重用收据；即使数据库其实已被创建，下次运行也会拒绝自动接管这个无凭证库。数据库被删除后重建为同名库时，OID 不匹配同样会拒绝恢复。本机 `debug` 后端在迁移前检查状态，无凭证的新空库和仍处于初始化中的库都不会被普通启动流程修改。迁移或写入期间断电后，只能用同一本机恢复收据继续。体验账号、层级、赔率、房间开关和完成凭证在同一个数据库事务内提交，不能只留下半套数据。完成后的 `dev-init` 只执行只读验收，若管理员已修改账号、赔率或房间配置则拒绝覆盖。

脚本会同时核对 TCP 端点、本机 checkpointer 进程，以及 TCP/Unix socket 两条连接返回的 PostgreSQL `system_identifier`、启动时间和版本，避免把 `127.0.0.1` 上的 SSH 隧道误当成本机数据库。当前用户能看见监听进程时，`lsof` 还会验证准确 host/port 的 LISTEN 进程确为 PostgreSQL；macOS 由专用系统用户运行 PostgreSQL 时，普通用户的 lsof 可能看不到它，此时仍以进程及双通道数据库身份校验为硬门禁。Docker/远端容器、Windows、关闭 Unix socket 或进程位于其他 PID namespace 的 PostgreSQL 会按安全原则被拒绝；此流程当前面向 macOS/Linux 的本机原生 PostgreSQL。已有且没有初始化凭证的数据库不会被接管，即使它看起来是空库；请指定一个尚不存在的新库名。数据库必须由当前 PostgreSQL 用户创建并拥有，才能保存初始化状态。不要把正式库或正在使用的业务库交给此命令。

需要自定义连接时可以直接设置 `BACKEND_*`，或传入一个只包含 `BACKEND_*` 的本机环境文件：

```bash
BACKEND_DATABASE_DBNAME=wangzhe_local make dev-init
make dev-init ENV_FILE=/绝对路径/local-backend.env
make dev ENV_FILE=/绝对路径/local-backend.env
```

环境文件必须是当前用户拥有的普通文件，权限为 `600` 或 `400`，不能是符号链接。脚本把它作为数据解析，不会 `source` 或执行其中的 `$()`、反引号等内容。初始化和启动只接受 `debug`、本机回环 PostgreSQL 及独立应用数据库；`postgres`、`template0`、`template1` 会被拒绝。

仓库提供了可复制的最小环境文件样例。复制后必须先改权限，再按本机 PostgreSQL 修改账号、密码或库名；使用本地 trust/peer 认证时密码可显式留空：

```bash
cp deploy/env/local-backend.env.example .env.local
chmod 600 .env.local
make dev-init ENV_FILE="$PWD/.env.local"
make dev ENV_FILE="$PWD/.env.local"
```

`wangzhe` 是数据库名称，不是数据库用户名称。更换默认值不会重命名或清空已有数据库；迁移旧本地库时先备份、停止相关服务，完成数据库重命名后同步显式环境配置。不要用开发重置脚本代替数据库重命名。

默认地址：

- 用户端：`http://127.0.0.1:5173`
- 管理/代理后台：`http://127.0.0.1:5174`
- 后端：`http://127.0.0.1:8080`

同一局域网的手机可把 `127.0.0.1` 替换为这台电脑的局域网 IP。默认情况下用户端和后台按浏览器正在访问的主机请求 8080 端口；`make dev` 会在自定义 `BACKEND_SERVER_PORT` 时同步注入端口，所以手机不会请求自己的 `127.0.0.1`。三个开发服务按既定需求监听 `0.0.0.0`，但启动探针使用的 `BACKEND_URL`、`MEMBER_URL`、`ADMIN_URL` 必须是端口匹配的 loopback HTTP origin，不能继承生产域名或带路径/凭据的地址；残留的 `VITE_API_BASE_URL` 会被清除。前端工具链进程会动态移除全部 `BACKEND_*` 环境变量，因此数据库密码、JWT 密钥、字段加密密钥及以后新增的后端秘密都不会传给 npm/Vite。`make dev` 只有在目标数据库属于当前本机 PostgreSQL 集群并带有有效 `complete` 凭证时才会启动。任一端口已被占用时脚本会拒绝启动，避免把另一个后端或另一套数据库误判为本次服务。

## 本地账号与层级

- 体验会员：`wangzhe88 / Wz888888`，默认房间 `88001`
- 管理员：`admin / Admin8801!`
- 租户：`wangzhetenant / WzTenant8801`
- 88001 房间代理：`suyang / Room8801`，所属租户 `wangzhetenant`

`make dev-init` 在全新库中建立一个平台、一个租户工作区、一个代理工作区和准确的一条会员归属链；会员 `wangzhe88` 位于代理 `suyang` 的 `88001` 房间。账号、工作区、有效成员关系、上下级外键和余额流水在同一次验收中核对。管理中心只需账号、密码，身份和归属由后端识别；登录账号全局唯一（不区分大小写）。

普通 `make dev` 只启动服务，不创建体验账号、房间、赔率或模拟开奖记录。体验开关默认为 `false` 且仅允许 `debug`；`test` 或 `release` 模式启用会直接失败。初始化只补缺失对象，不重置密码、不启用已停用账号，也不会把已占用的 `88001` 房间交给另一个账号。

## 代码、环境与数据库的边界

| 位置 | 应保存的内容 | 不应保存的内容 |
| --- | --- | --- |
| Git 代码 | SQL 迁移、玩法目录与结算算法、彩种/开奖源适配、基础分类规则、版本化的本地验收赔率配置 | 正式密码、令牌、线上连接串、实际注单和余额 |
| 环境变量/本机环境文件 | 数据库连接、服务端口和模式、JWT/字段加密密钥、Redis、CORS 等机器或部署差异 | 表结构、会员资料、注单、后台可编辑赔率 |
| 初始化后的数据库 | 迁移版本、初始化状态凭证、账号密码哈希、工作区与成员关系、彩种目录/分类、已确认赔率与限额、房间开关和基础设置 | 明文密码、由启动过程反复覆盖的后台配置 |
| 运行中的数据库 | 期号与开奖结果、注单与结算、余额流水、返水/代理分成、消息、操作与源异常日志 | 应由迁移和规则版本管理的表结构或结算代码 |

本地体验账号的明文凭据只用于 `debug` 夹具定义，数据库只保存密码哈希。数字赔率不会由普通后端启动自动生成；`make dev-init` 才会把受版本控制的 1437 项本地验收赔率显式写成已确认配置，并为 `88001` 房间打开对应 22 个彩种。之后后台修改的赔率、限额和开关都以数据库为准，重启不会重新套用验收配置；再次执行验收发现差异会停止，而不是覆盖管理员的修改。生产环境必须由后台或受审计的运营流程配置赔率，不能依赖本地验收配置。

### 使用已有本地账号预填

在 `new-back/.env.local` 设置本机专用覆盖，例如：

```dotenv
# 请替换为当前本地数据库已存在的代理账号
VITE_DEV_AGENT_USERNAME=local_agent
VITE_DEV_AGENT_WORKSPACE=平台
```

支持 `VITE_DEV_PLATFORM_*`、`VITE_DEV_TENANT_*`、`VITE_DEV_AGENT_*` 三组变量，每组均可设置 `USERNAME`、`PASSWORD`、`WORKSPACE`。未设置的字段沿用上面的默认值，显式空值会清空相应预填；账号密码不同时也要设置对应的 `PASSWORD`。修改后重启管理端开发服务。

这些变量只在 Vite 开发模式填充表单，不创建、重命名账号，不修改密码、角色、租户归属或房间历史；生产构建不启用预填。`VITE_*` 值对浏览器可见，因此只可填写本地测试凭据，不要放正式账号密码，也不要提交 `.env.local`。

## 默认分类与房间开关

普通首次启动会初始化基础彩种目录与分类，但不会制造数字赔率。显式的 `make dev-init` 会在空库目录完成后写入本地验收赔率；生产赔率仍由后台确认。内置 30 个彩种中，仅下列 22 个具有默认分类，其余 8 个官方彩保持“未分类”。分类是正式基础配置，和仅在 `debug` 模式提供的体验账号、验收赔率无关。

| 默认分类 | 彩种（按默认顺序） |
| --- | --- |
| 彩票（8 个） | 极速赛车、极速飞艇、极速时时彩、SG飞艇、SG时时彩、幸运飞艇、澳洲幸运5、澳洲幸运10 |
| PC（3 个） | PC加拿大、加拿大28、加拿大2.0 |
| 宾果（7 个） | 宾果六合彩、宾果赛车(A)、宾果赛车(B)、宾果时时彩(一)、宾果时时彩(二)、宾果时时彩(三)、宾果时时彩(四) |
| 六合彩（4 个） | 香港六合彩、快乐8六合彩、新澳门六合彩、老澳门六合彩 |

福彩3D、福彩快乐8、排列3D、七星彩、台湾宾果、台湾威力彩、台湾今彩539、台湾大乐透默认未分类，不因其玩法名称自动归入上述分类；仍可在后台游戏列表查看并自行配置。

- 平台统一管理分类、排序和总开关。新库的平台目录默认启用 22 个彩种，8 个官方彩保持关闭且未分类，接入、分类并确认后再开启。
- 新租户的直属房间、新代理房间自动沿用完整的平台目录和分类，但房间的每个彩种开关默认关闭。进入该房间的“游戏列表”后按需开启，不需要再次分类。
- 房间开关彼此独立。租户开启自己的直属房间，不会同时开启下属代理房间；平台后续新增的彩种也不会自动在已有房间开放。
- 会员实际可见需要同时满足：房间有效、平台已开启、彩种已分类、该房间已开启。后台“已开放”数量按实际可用状态统计，不只看房间开关。
- 旧版初始化迁移补齐分类并将已有房间原先隐含的开关状态显式保存；新的分类修正迁移仅移除上述 8 个官方彩匹配旧默认分类与排序的归属，恢复为未分类、排序 0。已有游戏开关、房间开关及不同于旧默认的自定义归属或排序保持不变；迁移后的手动分类调整不会被重启覆盖。

一般新建房间仍遵循“已有默认分类、游戏默认关闭”的规则。`make dev-init` 创建的 `88001` 验收房间是明确例外：命令在赔率和规则核验通过后打开上述 22 个彩种，使另一台电脑首次启动即可进入逐彩种下注验收。

## 开奖与封盘计时

开奖周期、封盘提前量和采集检查间隔是三个不同的时间。开奖周期优先采用源站下一期时间，并用连续有效的开奖记录校正；没有可靠源站时序时才使用目录配置。后台“开奖管理”显示当前采用的周期、封盘提前量以及精确到秒的北京时间，不把“几秒后检查”当成开奖倒计时。

平台“系统设置 → 游戏设置 → 封盘秒数”同时作为平台自身设置和新房间的初始化默认值。创建租户直属房间、代理房间或首次补建缺失的房间设置时，会复制当时的平台封盘配置（含有效的逐彩种封盘配置）；平台未设置时使用默认 30 秒。

这是**初始化时复制一次，不是运行时全局联动**。已有房间的设置不会被平台后续修改或服务重启覆盖；租户、代理可在各自“房间设置”中修改封盘秒数，逐彩种覆盖仍然保留。比如平台先设 45 秒，新建房间初始为 45 秒；一个房间改为 10 秒后，其他房间不变；平台后来改为 20 秒，只影响平台自身及以后新建房间，已有房间继续保留 45 秒或 10 秒。游戏开关、机器人开关和其他房间结构默认值不因复制封盘配置而改变。

实际封盘提前量读取当前房间的 `seal_seconds`。例如一期 75 秒、提前 30 秒封盘时，会员端先计时最多 45 秒，归零后切换为 30 秒的封盘计时，再进入“开奖中”。大厅卡片和游戏页都不显示“受理倒计时”文字，仅向读屏保留完整状态。游戏页顶部只保留期号和数字，封盘时在秒数右侧显示“封盘中”，封盘倒计时归零后同位置显示“开奖中”。源站延迟时不在本地循环倒计时、不虚构下一期，也不重新开放旧期。

换期刷新不再只依赖 WebSocket：连接正常时仍有 30 秒兜底，当前游戏在封盘/开奖边界主动核对，临近开奖而仍等待时每 2 秒补拉；长时间异常和请求失败会退避，页面隐藏时暂停补拉、恢复可见时立即校时和刷新。目录请求单飞合并，旧房间响应会取消并丢弃；校时与目录请求独立，校时暂时失败不会吞掉已经返回的新期号。聊天时间线和开奖图片与每秒倒计时隔离，仅实际业务数据变化时更新，不依赖新增性能库。

源同步会先原子保存已验证的开奖结果、下一期期号/时间及生命周期，提交后再发布目录更新并结算旧期，避免通知触发的读取落到旧期。168 高频彩种有完整下一期时间时，最新结果先处理，随后再补今天/昨天历史；补历史不能回退当前排程或清除实时源错误。缺少可靠下一期时间时仍需足够历史证据，不使用任意种子周期。采集仍保持原有 15 秒组调度和请求上限，因此“前端可及时恢复”不代表上游结果没有发布/采集延迟。

每个房间、彩种、期号保存独立的受理窗口，真正的下单和撤单都在后端校验。已记录的当期截止时间只能提前，不能因为改配置而延后；减少封盘提前量等延长受理的修改在下一期生效。开奖时间和已经收到的开奖结果分别保存，不改写历史注单或余额。

游戏页的“咪牌”工具使用已公布的期号与开奖号码，号码单行排列，可用手指或鼠标刮开覆盖层，也可选择全部揭晓或重新刮开；不会提前展示未开奖期号、生成新结果或影响结算。沿用原 `show_mipai_tool` 开关，后台显示名称为“咪牌”。回到最新消息使用纯图标小按钮，保留读屏名称和扩大点击区域。

为后续逐个彩种调整预留了设置请求中的 `game.game_timing_overrides[gameId].seal_seconds`；未配置时沿用该房间的 `game.seal_seconds`。这是现有设置接口的可选字段，暂未新增逐彩种编辑页面。封盘提前量须为 0 至 86400 的整数，`0` 表示开奖时才封盘。

## 计划群与自动推荐

总管理员在“内容与服务 → 计划管理”（`/plans`）按房间配置；会员从“聊天 → 计划群”（`/messages/plans`）查看。原公告页面的手工发布入口保留，租户、代理只能管理各自房间的手工推荐，不能访问总管自动化配置接口。

自动化默认关闭，启动不再给全部房间批量添加计划。管理员保存启用只允许访问时生成，不启动后台定时产出；会员打开且页面可见时，每15秒通过POST请求当前彩种/名次/计划。无访问即无生成，隐藏、离开页面或切换房间会取消旧请求。当前版本根据房间、真实期号和专家标识生成号码，并非真实预测算法或外部专家数据源。界面统一使用“1号专家、2号专家、3号专家”，说明为“系统自动生成，仅供娱乐参考，不保证命中。”不会在开奖后补造推荐或预设80%–90%命中率。

极速赛车支持冠军至第十名，默认“冠军 · 四期五码”，会员通过“切换计划”选择名次与计划：

- 号码：四期/三期五码、四期/三期六码、四期/三期七码、二期/一期八码；号码为1–10内不重复候选数。
- 方向：大小、单双、龙虎各提供五期、四期、三期。1–5为小、6–10为大；龙虎比较所选名次与对位名次（第1与第10、第2与第9等）。
- N期表示同组推荐跨N个实际确认开放的期次，再生成下一组。不会预造未来期号；中断期间漏过实际期次会重新起组，不事后补造推荐或计算命中率。

总管理员可按房间限定允许的名次与计划类型；新房间自动化仍默认关闭。GET始终只读；默认冠军与其他组合都必须由可见详情页POST触发，不常驻、不因未完成周期而后台续写。多个用户共享同一房间、彩种、名次、计划的结果，不按用户复制记录。服务端同流5秒合并请求，60秒访问租期只用于限制活跃组合数量，不代表定时运行；每房间最多20个活跃组合。激活接口按会员与房间共享Redis限流，每分钟最多30次。每个周期存储3位专家推荐，每期仅增加周期引用。关闭配置、移除彩种或取消组合授权后停止推进，重新放开不自动复活旧订阅。

其他彩种仍生成3个号码，号码范围为赛车/飞艇/幸运10的1–10、时时彩/幸运5的0–9、PC的0–27。原手工极速赛车发布入口仍是单期五码，复杂周期计划由自动配置管理。旧版3码/5码历史保留原号码、期号和时间，不回填、不混入新名次/计划组合的历史；已知旧自动专家名称在读取时统一展示为新名称。内部`mode/source`保持不变，不表示真实外部专家或验证过的预测。

生成还必须满足平台与房间游戏已开放、房间计划功能启用、上游明确提供下一期期号和时间（`timing_source=upstream`）、实际受理窗口未封盘。配置周期或从历史推测下一期的彩种暂不自动生成；不会为了填充界面而制造期号。

会员端默认仅显示最近6期，接口最多允许10期。极速赛车每个组合最多保存最近20期，发布时清理更老的计划明细和无引用周期，但保留当前周期的期号/时间高水位，避免清理后旧期复活。此清理不涉及注单、余额流水或开奖结果；旧单期推荐暂仅限制读取，其原记录和幂等凭证保留。后台旧“生成预览并发布”已取消，不能脱离会员访问批量写入。封盘或换期空档只展示真实历史，不把上一期当作本期推荐；没有提前发布的历史时显示空状态。

独立 PostgreSQL 回归（仅空的本机 `wangzhe_timing_test`，测试全事务回滚）：

```bash
cd backend
BACKEND_TIMING_TEST_DSN='postgres://test_user:TEST_PASSWORD@127.0.0.1:55439/wangzhe_timing_test?sslmode=disable' go test ./services -run '^TestPlan(Automation|Streams)Postgres' -count=1
```

## 验证

```bash
make health
make dev-audit
make verify
```

`make health` 在服务运行时检查 PostgreSQL、三个服务和 22 个启用彩种。`make dev-audit` 是纯数据库只读验收，不是 HTTP smoke：它不要求服务已启动，不绕过验证码，也不读取登录令牌；它在 PostgreSQL 强制只读事务中检查账号/工作区/成员关系拓扑、余额流水算术与连续性、异常对账注单、精确的 1437 项已确认赔率、会员赔率系数、实际可下注的 22 个彩种及 `88001` 房间开关，不执行迁移或任何修补。升级库可以保留验收范围外的旧赔率占位行，但这些行必须完整保持未确认、无规则版本、`unconfigured` 来源且无确认时间；任何已配置行、非惰性旧默认、重复键或验收范围内的目录外行都会使审计失败。历史兼容命令名 `make smoke` 目前指向同一项数据库只读验收。该验收只证明当前数据库状态与受版本控制的本地验收配置一致，不证明它最初由 `make dev-init` 创建、中间未被复制或其他历史来源；初始化来源还需根据数据库的 `complete` 凭证与受控备份/运维记录另行核对。普通已有业务库不应套用本地验收配置。`make verify` 执行全部 Go 测试和两个前端的 lint/build。

业务重置完成并通过正常图片验证码登录生成 owner-only cookie jar 后，可执行实际 HTTP 逐彩种验收：

```bash
make dev-lottery-acceptance \
  RESET_RECEIPT=/绝对路径/business.sql.gz.reset-receipt \
  COOKIE_JAR=/绝对路径/member.cookies
```

该命令默认精确校验 22 个本地验收彩种，每个彩种至少提交两注，并校验原子扣款和幂等重放。命令本身不清理数据、不改彩种或赔率；如需只验收指定彩种，可通过 `ACCEPTANCE_ARGS='--game <game-id>'` 传入。

分类初始化、增量迁移、重启保留配置和新房间默认关闭另有真实 PostgreSQL 集成测试：

```bash
BACKEND_CATALOG_TEST_DSN='postgres://test_user:TEST_PASSWORD@127.0.0.1:55439/wangzhe_catalog_test?sslmode=disable' make catalog-integration-test
```

必须先创建独立的空数据库 `wangzhe_catalog_test`，连接账号须能在其中创建表、函数和触发器。测试拒绝非本机地址及非空库，不读取本机 `config.yaml`，结束时回滚整个测试 schema；不要填写日常开发库或生产库的连接信息。测试用账号与密码自行设置，URL 中的特殊字符需编码。

开奖、封盘、后台开户和本地验收事务另有真实 PostgreSQL 集成测试，覆盖迁移幂等、平台默认值的一次性初始化、后续房间配置独立、独立房间窗口、下单/撤单的封盘边界、管理员把新会员原子绑定到指定代理房间，以及体验账号/赔率/完成凭证任一步失败时整笔回滚：

```bash
BACKEND_TIMING_TEST_DSN='postgres://test_user:TEST_PASSWORD@127.0.0.1:55439/wangzhe_timing_test?sslmode=disable' make timing-integration-test
```

同样只允许独立、本机、空的 `wangzhe_timing_test` 数据库，完成后回滚测试 schema；不使用业务库做下注或改余额验证。

## 查账与利润

管理员登录 `http://127.0.0.1:5174` 后进入“财务报表”，可以按日期、房间、彩种、会员和汇总维度查询，并可展开到每一张已结算注单。页面中的统一口径为：

```text
毛利 = 已结算流水 - 派彩
平台净利润 = 毛利 - 返水 - 福利成本 - 代理分成
会员输赢 = 派彩 - 已结算流水
```

“每日代理分账”显示每个房间的应计、已付和待付金额。管理员确认后执行分账；同一天重复执行只补发新增的未付差额，不会重复入账。每次付款都会生成 `agent_share` 余额流水和永久分账记录，可通过流水编号交叉核对。

代理登录同一后台后只能看到自己房间的经营报表和分账记录，不能执行分账，也不能访问其他房间或管理员接口。
