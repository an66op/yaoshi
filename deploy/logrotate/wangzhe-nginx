/var/log/nginx/wangzhe-access.log {
    daily
    rotate 14
    compress
    delaycompress
    missingok
    notifempty
    create 0640 www-data wangzhe-monitor
    sharedscripts
    postrotate
        [ ! -s /run/nginx.pid ] || kill -USR1 `cat /run/nginx.pid`
    endscript
}
