# 王者本地开发

## 一键启动

先启动本机 PostgreSQL，并确认 `backend/config/config.yaml` 中的数据库连接可用，然后在仓库根目录执行：

```bash
make dev
```

固定地址：

- 用户端：`http://127.0.0.1:5173`
- 管理/代理后台：`http://127.0.0.1:5174`
- 后端：`http://127.0.0.1:8080`

同一局域网的手机可把 `127.0.0.1` 替换为这台电脑的局域网 IP。用户端和后台会自动请求同一主机的 8080 端口。

## 本地账号

- 体验会员：`wangzhe88 / Wz888888`，默认房间 `8801`
- 管理员：`admin / Admin8801!`
- 8801 房间代理：`suyang / Room8801`

## 验证

```bash
make health
make smoke
make verify
```

`make health` 检查 PostgreSQL、三个固定端口和 22 个启用彩种；`make smoke` 验证会员房间、管理员对账以及代理权限边界；`make verify` 执行全部 Go 测试和两个前端的 lint/build。

## 查账与利润

管理员登录 `http://127.0.0.1:5174` 后进入“财务报表”，可以按日期、房间、彩种、会员和汇总维度查询，并可展开到每一张已结算注单。页面中的统一口径为：

```text
毛利 = 已结算流水 - 派彩
平台净利润 = 毛利 - 返水 - 福利成本 - 代理分成
会员输赢 = 派彩 - 已结算流水
```

“每日代理分账”显示每个房间的应计、已付和待付金额。管理员确认后执行分账；同一天重复执行只补发新增的未付差额，不会重复入账。每次付款都会生成 `agent_share` 余额流水和永久分账记录，可通过流水编号交叉核对。

代理登录同一后台后只能看到自己房间的经营报表和分账记录，不能执行分账，也不能访问其他房间或管理员接口。
