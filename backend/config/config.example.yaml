server:
  bind: 127.0.0.1
  port: 8080
  mode: release
  # Debug-only, one-time fixture provisioning. Keep disabled for normal starts.
  seed_experience_accounts: false
  # Debug-only synthetic draw fixtures. Normal starts and resets keep this false.
  seed_deterministic_lottery_history: false
  # Add the actual frontend origin(s), including scheme and port when applicable.
  allowed_origins:
    - https://example.com
  # For a local Nginx reverse proxy, use 127.0.0.1.
  trusted_proxies:
    - 127.0.0.1

database:
  host: 127.0.0.1
  port: 5432
  user: postgres
  password: CHANGE_ME
  dbname: wangzhe
  sslmode: require
  # Budget these values across all backend replicas and PostgreSQL reserved connections.
  max_idle_conns: 10
  max_open_conns: 50
  conn_max_lifetime_seconds: 3600

redis:
  addr: 127.0.0.1:6379
  username: ""
  password: ""
  db: 0
  tls: false
  prefix: wangzhe

jwt:
  # Release mode requires at least 32 random characters.
  secret: REPLACE_WITH_A_RANDOM_SECRET_AT_LEAST_32_CHARACTERS
  # Token lifetime in seconds (24 hours).
  expire: 86400

security:
  data_encryption_key: REPLACE_WITH_AN_INDEPENDENT_RANDOM_32_BYTE_SECRET
  # During a controlled rotation, keep old production secrets here until an
  # inventory confirms that no enc:v1 or old-key enc:v2 values remain.
  # New writes always use data_encryption_key; this list is decrypt-only.
  data_encryption_previous_keys: []
